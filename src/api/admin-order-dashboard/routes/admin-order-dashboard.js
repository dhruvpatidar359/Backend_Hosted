'use strict';

/**
 * admin-order-dashboard router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::admin-order-dashboard.admin-order-dashboard');
